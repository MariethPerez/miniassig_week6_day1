<%= form_with(model: actor, local: true) do |form| %>
  <% if actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor.errors.count, "error") %> prohibited this actor from being saved:</h2>

      <ul>
      <% actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :name_real %>
    <%= form.text_field :name_real %>
  </div>

  <div class="field">
    <%= form.label :fecha_nac %>
    <%= form.date_select :fecha_nac %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
