<%= form_with(model: cassete, local: true) do |form| %>
  <% if cassete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cassete.errors.count, "error") %> prohibited this cassete from being saved:</h2>

      <ul>
      <% cassete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :formato %>
    <%= form.text_field :formato %>
  </div>

  <div class="field">
    <%= form.label :pelicula_id %>
    <%= form.text_field :pelicula_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
